Postgres Sever
    Password: 247Posgres?
pgAdmin
    Password: 247pgAdmin?

drivers to connect to postgres
    pip install psycopg2
    pip install psycopg2-binary

To use ImageField, need to have a dependency 'Pillow'
    pip install Pillow

To make migrations
    python manage.py makemigration (creats the file)
    python manage.py migrate
    
Activating the virtual env
    source venv/bin/activate # from the folder that you want
